// NAME: Michael Liu
// ORGN: MIT
// FILE: pCommunicationAngle_liumk.moos
// DATE: 25 FEB 2020

//------------------------------------------
// MOOS File
	
  //Host = oceanai.mit.edu
  //Port = 9011

//------------------------------------------
// MOOS File - Local Community Stub

ServerHost = localhost
ServerPort = 9011

//------------------------------------------
// Antler configuration  block
ProcessConfig = ANTLER
{
  MSBetweenLaunches = 200

  Run = MOOSDB                      @ NewConsole = false
  Run = pCommunicationAngle_liumk   @ NewConsole = true
  Run = uXMS                        @ NewConsole = true
}

//------------------------------------------------
// pCommunicationAngle_liumk config block

ProcessConfig = pCommunicationAngle_liumk
{
   AppTick   = 4
   CommsTick = 4
}

//------------------------------------------------
// uXMS config block

ProcessConfig = uXMS
{
   AppTick   = 4
   CommsTick = 4

  var = ACOUSTIC_PATH, CONNECTIVITY_LOCATION
	//var = NUM_VALUE, PRIME_RESULT

}













	// var = VEHICLE_NAME, COLLABORATOR_NAME, NAV_X, NAV_Y, NAV_DEPTH, NAV_HEADING, NAV_SPEED, NEPTUNE_NAV_X, ....., ACOUSTIC_PATH_REF, CONNECTIVITY_LOCATION_REF
	// var = DISTNACE, CENTER, RADIUS, ARC LENGTH, ELEV_ANGLE, R_S, Z_S, NEW_RADIUS, R_S1, GRAZING_ANGLE, TRANSMISSION_LOSS, ACOUSTIC_PATH, CONNECTIVITY_LOCATION, ADJUSTED_RADIUS/X/Y
	//COLOR_MAP = DISTANCE, RED
	//COLOR_MAP = CENTER, GREEN
	//COLOR_MAP = RADIUS, BLUE
	//COLOR_MAP = ARCLENGTH, GREEN
	//COLOR_MAP = ELEV_ANGLE, RED
	//COLOR_MAP = R_S,BLUE
	//COLOR_MAP = Z_S,BLUE
	//COLOR_MAP = J_S,GREEN
	//COLOR_MAP = TRANS_LOSS,RED
